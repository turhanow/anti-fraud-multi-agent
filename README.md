# 🧠 Multi-Agent Fraud Detection System

## Architecture & Agent Responsibilities

---

## 1. Цель системы

Данная система предназначена для обнаружения мошеннических финансовых транзакций на основе мультиагентной архитектуры.

**Ключевая идея:**
> Не существует одного универсального сигнала мошенничества.  
> Решение формируется как результат независимого анализа нескольких агентов,
> каждый из которых отвечает за отдельный аспект риска.

**Система ориентирована на:**
- explainable AI (объяснимость решений)
- расширяемость (добавление новых агентов без переписывания системы)
- enterprise-ready подход (финтех, банки, e-commerce)

---

## 2. Архитектурный принцип

### 2.1 Почему мультиагентная архитектура

**Классический ML-подход:**
- один большой модельный пайплайн
- сложная отладка
- слабая объяснимость

**Наш подход:**
- каждый агент — автономный аналитик
- агенты независимы и специализированы
- финальное решение агрегирует сигналы

---

## 3. Основные компоненты системы

```text
┌──────────────────────────┐
│      Orchestrator        │
│  (Control & Dispatch)    │
└───────────┬──────────────┘
            │
 ┌──────────┼──────────┬──────────┐
 │          │          │          │
Velocity  Profile     Geo       Device
 Agent     Agent      Agent      Agent
 │          │          │          │
 └──────────┼──────────┼──────────┘
            │
     Merchant Agent
            │
        ML Agent
            │
     Decision Agent
```

## 4. Domain Model (основа всей системы)

### 4.1 Transaction (центральный объект)

Все агенты работают не с raw CSV, а с единым объектом `Transaction`.

**Пример логических групп данных:**

- **Идентификация**
    - `transaction_id`
    - `customer_id`
    - `card_number` (masked)
- **Временные характеристики**
    - `timestamp`
- **Финансы**
    - `amount`
    - `currency`
    - `card_type`
    - `card_present`
- **Мерчант**
    - `merchant`
    - `merchant_category`
    - `merchant_type`
    - `merchant_risk_score`
- **География**
    - `country`
    - `city`
- **Устройство**
    - `device_type`
    - `device_fingerprint`
- **Профиль клиента**
    - `account_age`
    - `typical_spending_range`
    - `preferred_devices`
    - `fraud_protection_enabled`

## 5. Агентная модель

### Общий контракт агента

Каждый агент:
- получает объект `Transaction`
- возвращает структурированный сигнал риска

**Стандарт ответа агента:**
```json
{
  "agent": "VelocityAgent",
  "score": 0.0..1.0,
  "risk_level": "LOW | MEDIUM | HIGH",
  "explanation": "Human-readable explanation",
  "features_used": ["feature_1", "feature_2"]
}
```

## 6. OrchestratorAgent

- **Роль**: Центральный координатор системы.
- **Ответственность**:
    - управляет жизненным циклом анализа
    - рассылает транзакцию агентам
    - собирает результаты
    - передаёт агрегированные сигналы в `DecisionAgent`
- **Почему он нужен**:
    - изолирует агентов друг от друга
    - позволяет легко добавлять / убирать агентов
    - упрощает масштабирование

## 7. VelocityAgent

- **Назначение**: Анализ динамики транзакций во времени.
- **Основной вопрос**: Насколько поведение клиента сейчас отклоняется от его обычной транзакционной скорости?
- **Используемые фичи**:
    - `timestamp`
    - `customer_id`
    - `transaction_amount`
    - `transaction_count` (за окно времени)
- **Примеры правил**:
    - N транзакций за M минут
    - резкий рост суммы
    - аномально короткий интервал между операциями
- **Тип сигнала**: Поведенческая аномалия.

## 8. ProfileAgent

- **Назначение**: Сравнение текущей транзакции с историческим профилем клиента.
- **Основной вопрос**: Похожа ли эта транзакция на типичное поведение данного клиента?
- **Используемые фичи**:
    - `account_age`
    - `typical_spending_range`
    - `merchant_category`
    - `preferred_devices`
- **Примеры сигналов**:
    - сумма вне привычного диапазона
    - новая категория расходов
    - несоответствие устройства профилю

## 9. GeoRiskAgent

- **Назначение**: Оценка географических рисков.
- **Основной вопрос**: Насколько текущая локация соответствует обычной географии клиента?
- **Используемые фичи**:
    - `country`
    - `city`
    - `currency`
    - предыдущие локации клиента
- **Примеры сигналов**:
    - невозможное перемещение (travel velocity)
    - высокая риск-оценка страны
    - несоответствие валюты региону

## 10. DeviceAgent

- **Назначение**: Анализ устройств и fingerprints.
- **Основной вопрос**: Насколько доверенным является устройство, с которого выполнена транзакция?
- **Используемые фичи**:
    - `device_type`
    - `device_fingerprint`
    - `card_present`
- **Примеры сигналов**:
    - новый fingerprint
    - `card_not_present` + высокая сумма
    - несовпадение `device_type` с профилем

## 11. MerchantAgent

- **Назначение**: Оценка риска, связанного с продавцом.
- **Основной вопрос**: Насколько рискован сам мерчант, независимо от клиента?
- **Используемые фичи**:
    - `merchant`
    - `merchant_category`
    - `merchant_risk_score`
- **Примеры сигналов**:
    - мерчант с высокой историей fraud
    - нетипичный мерчант для клиента
    - подозрительные категории (gambling, crypto, etc.)

## 12. MLModelAgent

- **Назначение**: Статистическая оценка вероятности мошенничества.
- **Особенность**: ML-агент НЕ принимает финальное решение.
- **Используемые фичи**:
    - агрегированные engineered features
    - исторические labels (fraud)
- **Возможные модели**:
    - Gradient Boosting (XGBoost / LightGBM)
    - Isolation Forest
    - Autoencoder
- **Роль в системе**:
    - даёт вероятностный сигнал
    - усиливает rule-based агентов
    - не заменяет reasoning

## 13. DecisionAgent

- **Назначение**: Финальное принятие решения.
- **Основной принцип**: `DecisionAgent` не обучается. Он рассуждает.
- **Ответственность**:
    - агрегирует сигналы от агентов
    - применяет веса и пороги
    - формирует итоговое решение
    - генерирует объяснение
- **Итоговый результат**:
```json
{
  "fraud": true,
  "confidence": 0.82,
  "decision": "BLOCK",
  "explanation": [
    "Unusual transaction velocity",
    "New device fingerprint",
    "High-risk merchant category"
  ]
}
```

## 14. Ключевые архитектурные свойства

- Explainability first
- Agent independence
- Open/Closed principle
- ML as advisor, not dictator
- Enterprise compliance ready

## 15. Возможные расширения

- LLM Reasoning Agent
- Human Review Agent
- Feedback Learning Agent
- Adaptive Weighting
- Real-time streaming (Kafka)

## 16. Итог

Данная архитектура:
- отражает реальные fraud-платформы крупных финтех-компаний
- масштабируется от MVP до enterprise
- подходит для обучения, экспериментов и production
